# docs/

- `screenshot.png` — hero image used in the project [README](../README.md).
- `edge-cases/` — Level-1 documentation of billing edge cases: what each is and
  why it matters (not how Sutyr responds to them).

To regenerate the screenshot, capture `lab.sutyr.com` (or `localhost:3000`) with
the three-panel layout visible and browser chrome cropped, saved as
`screenshot.png` in this directory.
